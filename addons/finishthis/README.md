# finishthis
Opens and sets the content of the chat input without sending anything.
## Example usage
`/bind F12 /finishthis "/uw ad "` and then pressing F12 will open (if its not already open) and set the chat input to `/uw ad `  
at which point you can then type any destination alias and press enter to warp with Uberwarp plugin.
